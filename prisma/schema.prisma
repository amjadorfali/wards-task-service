generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model HealthChecks {
  id      Int     @id @default(autoincrement())
  userId  String
  enabled Boolean
  cron    String
  status  Int
}
